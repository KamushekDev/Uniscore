class Profile{
  Profile(this.username, this.subscribersCount, this.subscriptionsCount, this.photoUrl, this.backgroundPhotoUrl, this.info);
  final String username;
  final int subscribersCount;
  final int subscriptionsCount;
  final String? photoUrl;
  final String? backgroundPhotoUrl;
  final String? info;
}